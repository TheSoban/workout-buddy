import ExerciseForm from '../../../components/forms/ExerciseForm';

const ModeratorNewExercise = () => {
  return <ExerciseForm update={false} initialData={null} />
};

export default ModeratorNewExercise;
